# 📚 簿記3級クイズアプリ - 起動ガイド

このガイドは、IT初心者の方でも簡単にクイズアプリを起動できるように作成されました。

## 🎯 必要なもの

- パソコン（Windows、Mac、Linux）
- インターネット接続
- Node.js（バージョン18以上）

---

## 🚀 起動手順

### 1️⃣ Node.jsのインストール確認

ターミナル（コマンドプロンプト）を開いて、以下を入力：

```bash
node --version
```

✅ バージョン番号が表示されればOK（例: v18.15.0）
❌ エラーが出た場合 → https://nodejs.org/ から**LTS版**をダウンロードしてインストール

---

### 2️⃣ プロジェクトフォルダに移動

```bash
cd /home/user/kuroibot_premium
```

---

### 3️⃣ 必要なパッケージをインストール

```bash
npm install
```

⏳ 少し時間がかかります（1〜3分程度）

---

### 4️⃣ アプリを起動

```bash
npm run dev
```

✅ 成功すると以下のようなメッセージが表示されます：

```
▲ Next.js 14.2.33
- Local:        http://localhost:3000
```

---

### 5️⃣ ブラウザでアクセス

以下のURLをブラウザのアドレスバーに入力：

**クイズアプリ直接:**
```
http://localhost:3000/quiz
```

**メインページ経由:**
```
http://localhost:3000
```
→ 右上の青い「簿記クイズ」ボタンをクリック

---

## 🎮 クイズの使い方

### 設定画面

1. **問題数**: スライダーを動かして5〜20問を選択
2. **カテゴリー**: 学習したいカテゴリーをクリック
   - 仕訳
   - 勘定科目
   - 帳簿
   - 決算
   - 伝票
3. **難易度**: 易しい、普通、難しい、ミックスから選択
4. **「クイズを開始」をクリック**

### 問題画面

- 各問題の選択肢をクリックして選択
- 「解答する」ボタンをクリック
- ✓ 正解 / ✗ 不正解 が表示されます
- 解説を読んで理解を深める
- 「次へ」をクリックして次の問題へ

### 結果画面

- スコア、正答率、合計時間が表示されます
- 「解答を確認」で全問題の復習ができます
- 「もう一度挑戦」で同じ設定で再挑戦
- 「設定に戻る」で設定変更

---

## ⚠️ トラブルシューティング

### エラー: "port 3000 already in use"

別のアプリがポート3000を使用中です。以下を実行：

```bash
npm run dev -- -p 3001
```

その後、`http://localhost:3001/quiz` にアクセス

### エラー: "command not found: npm"

Node.jsがインストールされていません。
https://nodejs.org/ からインストールしてください。

### アプリが起動しない

1. ターミナルを閉じて、新しく開く
2. プロジェクトフォルダに移動
3. `npm install` を実行
4. `npm run dev` を実行

### ブラウザに何も表示されない

1. サーバーが起動しているか確認（ターミナルにメッセージが表示されているか）
2. URLが正しいか確認（http://localhost:3000/quiz）
3. ブラウザのキャッシュをクリア（Ctrl+F5 または Cmd+Shift+R）

---

## 🛑 アプリを停止する

ターミナルで **Ctrl + C** を押す

---

## 📞 サポート

問題が解決しない場合は、以下の情報を添えて質問してください：

1. エラーメッセージの全文
2. 使用しているOS（Windows、Mac、Linux）
3. Node.jsのバージョン（`node --version`の結果）
4. どの手順でエラーが発生したか

---

## 🎓 学習のヒント

- 最初は「易しい」難易度で10問から始めましょう
- カテゴリー別に学習すると効果的です
- 解説をしっかり読んで理解を深めましょう
- 間違えた問題は復習機能で何度も確認しましょう
- スコアが80%を超えたら次の難易度に挑戦！

---

頑張ってください！📚✨
